---
scope: Repository-local Knowledge Control Plane Impact Analysis
purpose: Build substrate for deterministic index, target resolution, analyzer pipeline, and KCP integration
altitude: feature
updated: 2026-09-04T01:05
---

- (decision by user) Create a feature-altitude architecture spine consuming SPEC-kcp-impact-analysis and preserving existing KCP authority boundaries.
- (decision by user) Use a layered deterministic pipeline: source authority, index, resolver, analyzer, report, and consumer adapters; no shared mutable graph.
- (decision by user) A1 index lifecycle: build impact-index.v1 per trusted repository commit and analysis-config revision; store immutable derived files under logs/ci/impact-analysis/indexes/<commit>/.
- (decision by user) Index reuse requires exact repository revision, source hashes, index schema, analyzer config revision, and clean trusted-ref evidence; mismatch invalidates and rebuilds or fails closed.
- (decision by user) A2 TargetResolver accepts type/id or explicit path, resolves exact canonical paths before constrained symbol lookup, and returns ambiguity instead of guessing.
- (decision by user) A3 scripts/python/analyze_impact.py is the consumer-facing entrypoint; it reads frozen KCP context or current publication read-only and writes the impact report without changing KCP state.
- (decision by user) Before coding, run Knowledge Freeze, then analyze_impact.py, then coding; before review, freeze review context, generate the report, then review reads both inputs.
- (decision by user) Impact index and report are derived evidence and never authority; ADR, Task, Contract, Decision, Freeze, publication, Locator, and review authority remain unchanged.
- (decision by user) V1 relation types are references, implements, inherits, consumes, binds, tests, and documents; dynamic dispatch, reflection, generated code, editor-only state, and external package graphs are unsupported.
- (constraint by user) Windows-only newrouge, Godot 4.5.1, C#/.NET 8, Python entrypoints, repository-relative paths, and logs/ci evidence rules remain binding.
- (constraint by user) Analyzer is read-only and fail-closed on dirty state, stale index, source hash mismatch, missing index, ambiguous target, unreadable source, or unsupported relation.
- (constraint by user) No Neo4j, SQLite graph database, Roslyn full compiler integration, Godot editor plugin, visual dependency graph UI, or autonomous refactoring in v1.
- (assumption by user) Index retention and garbage-collection policy is not required for the first architecture slice and remains deferred.
- (question by user) Decide whether impact reports should be retained indefinitely or bounded by a repository evidence retention policy.
- (question by user) Decide whether review consumers receive a direct frozen-context path or only the report path in a later sidecar integration.
- (event by user) Architecture distillation selected as headless express run because SPEC and A1-A3 design requirements were fully supplied.
- (event) Architecture spine and architecture-details companion rendered from memlog with A1 index lifecycle, A2 TargetResolver, and A3 KCP insertion points.
- (decision by coach) Tighten index identity: index_id includes repository commit, source manifest hash, index schema revision, analyzer implementation revision, and analyzer config revision; storage nests commit/config identity.
- (decision by coach) Use repository evidence layout logs/ci/<YYYY-MM-DD>/impact-analysis/<run-id>/ for reports and logs/ci/<YYYY-MM-DD>/impact-analysis/indexes/<commit>/<config-revision>/ for immutable indexes; no fixed-path overwrite.
- (decision by coach) TargetResolver canonicalizes method signatures with namespace/type/name/parameter shape, treats aliases only as kind-scoped mappings, requires explicit paths for resources, and fails on overload ambiguity.
- (decision by coach) Impact edges carry from/to kinds, direction, relation, source path, anchor, and source hash; duplicate edges dedupe by canonical tuple.
- (decision by coach) KCP insertion points bind to existing commands: dev_cli.py resume-task and chapter6-route before run_single_task_light_lane.py, and review context freeze before run_review_pipeline.py; no implicit context expansion.
- (decision by coach) Operational envelope: local and CI read-only execution, atomic temp-to-final writes, per-run isolation, bounded scan scope, explicit exit statuses, cleanup of incomplete temp artifacts, and deferred retention owner.
- (constraint by coach) Trusted revision is bound by full commit SHA plus verified ref trust; a branch name alone is insufficient and feature-branch analysis must use an explicit revision.
- (constraint by coach) Impact scripts named in the architecture are Phase 1 deliverables and do not exist yet; architecture does not imply implementation completion.
- (event by coach) Reviewer Gate findings applied before polish; high findings on identity, resolver, relation semantics, KCP insertion, and operational envelope were resolved in the spine.
- (decision by coach) Close reviewer gap: source manifest now includes project.godot, NewRouge.csproj, Game.Core.csproj, test projects, and global.json with parser/kind/hash identity.
- (decision by coach) Close reviewer gap: report includes run-scoped path, index_id, index hash, analyzer implementation revision, KCP lineage, risk policy revision, and stable failure codes/exit behavior.
- (decision by coach) Close reviewer gap: explicit full revision plus verified ref trust is required; feature-branch analysis is allowed when revision and source mode are verified.
- (decision by coach) Close reviewer gap: path safety rejects absolute, drive-relative, UNC, traversal, symlink-escaping, and case-ambiguous targets.
- (event by coach) Reviewer findings resolved; rerun lint and independent reviewer gate before finalization.
- (decision) Fix executable failure code and Windows atomic publication contract; UUIDv4 run_id isolates retries and is excluded from deterministic comparisons.
- (decision) Bind physical index storage to full index_id at logs/ci/<YYYY-MM-DD>/impact-analysis/indexes/<index_id>/impact-index.v1.json; consumers verify index_id and artifact SHA-256.
- (decision) Publish the relation endpoint matrix and canonical consumes direction consumer -> event/contract; schema validation rejects unsupported endpoint combinations.
- (decision) Pin supported Python to 3.13.x via py -3, record resolved patch in toolchain identity, and require CI to pin the minor line.
- (decision) Define adapter ownership and argv forwarding for Chapter 6 and review entrypoints, with fail-closed tests before claiming integration is runnable.
- (event) Applied second reviewer convergence fixes for index path, relation matrix, knowledge binding, resolver grammar, adapter handoff, failure protocol, manifest policy, and retention ownership.
- (decision) Make knowledge_binding mandatory in successful coding/review reports with explicit null rules for review task_id and hash-bound freeze/publication lineage.
- (event) Architecture finalized after independent rubric, reality, and adversarial reviews; lint_spine passed with zero findings. Remaining adapter/CI work is explicitly deferred implementation gating.
- (event) Finalized companion alignment, JCS/golden-vector identity, explicit index/run manifests, Git-tree source mode, closed status/exit map, and deterministic Windows lock behavior.
